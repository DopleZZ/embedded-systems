server:
  port: 8080

mqtt:
  broker-uri: tcp://azat222.ru:1883
  client-id: fitocube-backend
  command-topic: soil/cmd
  data-topic: soil/data
  command-payload: get_info
  response-timeout: 5s

spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://azat222.ru:5555/embedded}
    username: ${DB_USERNAME:embedded}
    password: ${DB_PASSWORD:embedded}
  jpa:
    hibernate:
      ddl-auto: update
    defer-datasource-initialization: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: false
  sql:
    init:
      mode: always
      data-locations: classpath:data.sql
